How to installs php
-write the code in VS Code then run th code in XAMPP server.
-installs the XAMPP go to this website http://apachefriends.org/ and installs the VS Code go to this website https://code.visualstudio.com/download
-create a new file within our xampp folder.
-Go to Xammp, clck the htdocs, create new folder and select new folder the you create. After all of that within your new folder your well create a new file with name index.php if you see the notification click the 'open settings', click the 'edi in setthings.json' , if you see the "php.validat.executablePath":"" go to your xammp folder, go with php go down and see the php the right click, go to properties coppy the location, paste on executablePath and add php.exe and do the forward slashes '/' and save.
-go to extensions, find the 'PHP intellisense', 'live server' , and 'PHP server'.
-To accest your server go to browser type 'localhost/(name of your folder)/

PHP
PHP - popular general-purpose scripting language.
-build dynamic web page
-run on a server not the user's web browser.
-php is 77% used on all known website.
-speed, simplicity and flexibility.
-php is realeased in june 8, 1995 acronym for 'Personal Home Page' but now 'PHP Hypertext Processor'.
How does PHP work
-A browser sends a request to a server PHP on that server processes that request ther the server sends HTML back to the browser, our running PHP can even communicate to a database that back to the web browse.
-PHP is most commonly used with relational database such a MySQL, postgres and Oracle.
-PHP can be written alongside with HTML.

-to write php code type '<?php?>', to display a message type 'echo""/'';, you ccan add a '<br>- Break Tag' inside the message. To add a comment you can type '// - duble forward slashes' then some massage it's mostly used for notes, '/* (massage) */ - multi-line comment'. If you want to add a HTML you can type '!-exclamation point' and then tab.

Variable
-A reusable container that holds data string, integer, float and boolean.
to declare a varianble type '$ - dollar sign' the a unique variable name.
-String is series of text '&name = "Bro";' to display the output type 'echo $name;', if you want to display a message directly its call 'string literal - echo "hellow {$name}";' to add the variable name inside the message add 'curly braces-{variable name}'.
-Integer is a whole numbers $age = 21; echo"you are {$age} year old;
-float is a floating point number it's a number that contains a decimal portion. $gpa = 2.5; echo"Your Grade point average is: {$gpa};
-Booleana are eaither true or false $employed = true;
-implicit also call 'juggling'

-Arithmetic PHP 
-Arithmetic operators +(addition), -(subtraction) , *(multiplication), /(division), **(power), %(modulus) example $x=10; $y=2; $z=null; $z = $x+$y; echo $z;
-Increment/decrement operators ++,-- $counter = 0;
$counter++; echo $counter;
-abd(-), round(), cell(), sqrt()
-Operator Precedence ()(parentheses), **(exponents), * / %,+ - $total = 1+2-3*4/5**6; echo $total;

-PHP files contain HTML, CSS, Java Script and PHP.
-To add live server exetension click the 3. on the browser find the setting and find the extensions, click the extensions find the open chrome web store and search live server, Go to extension selecy live server web extension, enable the live reload, the actual server address is 'localhost/(name of your folder)/' coppy and paste it on actual server, and to find the live server address go to vs code press the live button, there should be a new tab that popsup and coppy the address on the top and paste it on live server you need to restart your browser.

for each - paggagamit kalang ng array.
